import { styled, Box } from '@mui/material';

export const CollectiveInvitesSectionContainer = styled(Box)({
  width: '100%',
  display: 'flex',
  justifyContent: 'center',
  padding: '30px',
  minHeight: '40vh',
});
